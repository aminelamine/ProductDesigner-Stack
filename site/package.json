{
  "name": "pds-stack-site",
  "version": "0.1.0",
  "private": true,
  "description": "Documentation site for PDS Stack — the design-first AI workflow.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "pagefind --site .next/server/app --output-path out/_pagefind || echo 'pagefind indexing skipped (search index not built)'",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.1.0",
    "nextra": "^4.6.1",
    "nextra-theme-docs": "^4.6.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "4.1.12"
  },
  "overrides": {
    "zod": "4.1.12"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
