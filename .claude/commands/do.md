# /do — alias de /analyzer (ANALYZER, Product QA & CX)

> Alias de compatibilité. Commande canonique : `/analyzer`.
> Lis et applique **intégralement** `.claude/commands/analyzer.md`, puis traite `$ARGUMENTS`.
> Tu es ANALYZER. Préfixe tes messages par `[ANALYZER]`.

$ARGUMENTS
