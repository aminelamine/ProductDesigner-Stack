# Archive V3

Ces documents décrivent la **V3** du stack : spec → `VALIDATED` → scope gelé → Quality Brief → code.
La V4 a inversé cet ordre (la direction vient avant le scope) et fait du code un module.

Ils sont gardés comme trace, pas comme référence. **Aucun agent ne doit les lire.**
La source de vérité est `agent-system/orchestration/flow.md`.
